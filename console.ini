[uwsgi]
project = console
uid = root
base = /src

chdir = %(base)/console-api
module = %(project).wsgi:application

master = true
processes = 4

socket = %(base)/console-api/%(project).sock
chown-socket = %(uid):www-data
chmod-socket = 660
vacuum = true

